torch>=2.1.0
# required by training/model.py build_densenet AND by the venv health
# check in runpod_launch; torchgeo does not reliably pull it in.
torchvision>=0.16.0
transformers>=4.36.0
# MUST stay <3: mlflow 3.x removed the local filesystem tracking backend
# ('./mlruns' -> MlflowException: filesystem tracking backend is in maintenance
# mode), which is what configs set as mlflow.tracking_uri. An unpinned install
# pulled 3.x and crashed training on every pod before epoch 1.
mlflow>=2.10.0,<3
earthengine-api>=0.1.390
geopandas>=0.14.0
pandas>=2.1.0
numpy>=1.24.0
pyarrow>=14.0.0
pydantic>=2.5.0
pyyaml>=6.0
shapely>=2.0.0
scikit-learn>=1.3.0
torchgeo>=0.7.0
python-dotenv>=1.0.0
overpy
runpod